/*$(function(){
  var titleField = $('#title-field');
  var textField = $('#post');

  $('.post-form').on('submit', function(e){
    e.preventDefault();
    $.post('/api/create-post', {title: titleField.val(), text: textField.val()});
  });
});*/